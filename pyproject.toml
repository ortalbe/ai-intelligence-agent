[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "claude-virtual-env"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*", "config*"]
